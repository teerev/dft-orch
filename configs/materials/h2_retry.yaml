run:
  retries: 1

structure:
  path: "structures/h2_pbc.xyz"

calculator:
  xc: "PBE"
  charge: 0
  spin: 0
  scf:
    # Intentionally too small to force one SCF failure + retry.
    max_cycle: 1
    conv_tol: 1e-8
  pbc:
    enabled: true
    basis: "gth-szv-molopt-sr"
    pseudo: "gth-pbe"
    mesh: [15, 15, 15]
    kpts: [1, 1, 1]
    use_multigrid: true

relax:
  enabled: false

validate:
  require_scf_converged: true
  max_force: 1000000.0

